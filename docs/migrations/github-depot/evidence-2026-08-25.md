# GitHub and Depot migration evidence — 2026-08-25

Status: in progress. A pending item is a stop gate, not permission to infer
success.

## Authority and rollback boundary

- Source repository: `https://github.com/finitecomputer/finite-mono`.
- GitHub `main` baseline: `d821bc1626c21c2ab332e7534861e813e158bf88`.
- GitHub `production` baseline: `d6a8b424cb319fd6f8a8d217850c177f2caee6af`.
- GitHub remains authoritative for branches, tags, pull requests, rulesets, and
  issues throughout the CI migration.
- Rollback restores the previous GitHub Actions workflow and its deliberately
  scoped credentials or environment grants.

GitHub repository rulesets govern both protected branches:

- Ruleset `20000075` (`main`) requires a pull request and `CI gate`.
- Ruleset `21154891` (`production`) requires a pull request, `CI gate`, and
  `Plan production deploy`.

## External control planes

- Depot organization: `scthc5h66g` (`Finite`).
- Depot container-build project: `6d9vqbnzvh` (`default`).
- Depot repository integration: `finitecomputer/finite-mono`.
- Release repository: `https://github.com/finitecomputer/finite-releases`.
- Depot CI has an appropriately scoped `CACHIX_AUTH_TOKEN`; no secret value is
  recorded in source.

## Accepted evidence

- Native Depot pull-request workflows check out GitHub revisions and report
  their conclusions on GitHub pull requests.
- The full native Depot pull-request graph has completed successfully.
- Pull-request jobs configure Cachix in read-only mode.
- Linux x86_64 release workflows retain immutable asset and checksum contracts.
- macOS CLI and Electron publication remain disabled rather than cross-built.
- Release and GHCR production publication remain fail-closed behind explicit
  variables.

## Remaining gates

- Capture a trusted `main` Cachix upload followed by a warm-cache substitution.
- Prove GitHub pull-request failure, retry, cancellation, and concurrency
  behavior through Depot.
- Prove GitHub branch, tag, and manual-dispatch event behavior through Depot.
- Prove the bounded Release publisher and alias-only rollback canaries.
- Prove GHCR digest equality, platform, provenance, and anonymous pulls.
- Replace required GitHub Actions checks with the accepted Depot checks.
- Remove each superseded GitHub Actions workflow and Actions-only credential
  only after its post-removal Depot event succeeds.
