# Depot CI Migration
# Source: .github/workflows/phala-readonly-preflight.yml
#
# Changes made:
# - Changed GitHub runs-on labels to their Depot equivalents

name: Phala read-only preflight
run-name: Phala read-only preflight (${{ github.ref_name }})
on:
  workflow_dispatch:
    inputs:
      confirm_read_only:
        description: "Run authenticated GET-only shape, price, capacity, and inventory checks"
        required: true
        type: boolean
        default: false
permissions:
  contents: read
jobs:
  preflight:
    name: Pinned API and inventory preflight
    if: ${{ inputs.confirm_read_only }}
    runs-on: depot-ubuntu-latest # was: ubuntu-latest. Mapped standard GitHub runner to Depot equivalent.
    timeout-minutes: 20
    env:
      FC_RUNNER_PHALA_EXPECTED_WORKSPACE_ID: ${{ vars.PHALA_EXPECTED_WORKSPACE_ID }}
      FC_RUNNER_PHALA_EXPECTED_WORKSPACE_SLUG: ${{ vars.PHALA_EXPECTED_WORKSPACE_SLUG }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.93"
      - name: Require the non-secret identity fence
        run: |
          test -n "$FC_RUNNER_PHALA_EXPECTED_WORKSPACE_ID"
          test -n "$FC_RUNNER_PHALA_EXPECTED_WORKSPACE_SLUG"
      - name: Run fake-server and fixture contract tests
        run: cargo test --locked --package finite-saas-runner phala
      - name: Run typed GET-only preflight
        env:
          FC_RUNNER_PHALA_API_KEY: ${{ secrets.PHALA_CLOUD_API_KEY }}
        run: |
          test -n "$FC_RUNNER_PHALA_API_KEY"
          cargo run --locked --quiet --package finite-saas-runner -- phala-preflight \
            > phala-preflight.json
      - name: Validate the redacted summary contract
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path

          path = Path("phala-preflight.json")
          report = json.loads(path.read_text(encoding="utf-8"))
          expected = {
              "apiVersion",
              "workspaceIdentityVerified",
              "workspaceQuotaSufficient",
              "instanceType",
              "vcpu",
              "memoryMb",
              "diskSizeGb",
              "hourlyPriceUsdMicros",
              "providerReportedMaxInstances",
              "availableNodeCount",
              "finiteAppCount",
              "nonDeletedFiniteCvmCount",
              "finiteRevisionCount",
              "billableFiniteResourceCount",
          }
          if set(report) != expected:
              raise SystemExit("unexpected Phala preflight summary fields")
          if report["instanceType"] != "tdx.medium":
              raise SystemExit("unexpected Phala instance type")
          if not report["workspaceIdentityVerified"] or not report["workspaceQuotaSufficient"]:
              raise SystemExit("Phala workspace identity or quota was not verified")
          if (report["vcpu"], report["memoryMb"], report["diskSizeGb"]) != (2, 4096, 40):
              raise SystemExit("unexpected Phala resource shape")
          if report["hourlyPriceUsdMicros"] != 116000:
              raise SystemExit("unexpected Phala hourly rate")
          expected_billable = max(report["finiteAppCount"], report["nonDeletedFiniteCvmCount"])
          if report["billableFiniteResourceCount"] != expected_billable:
              raise SystemExit("inconsistent Phala billable-resource aggregate")
          if report["billableFiniteResourceCount"] > 1:
              raise SystemExit("Phala billable-resource cap is exceeded")
          PY
      - name: Upload redacted preflight summary
        uses: actions/upload-artifact@v4
        with:
          name: phala-readonly-preflight
          path: phala-preflight.json
          if-no-files-found: error
          retention-days: 14
      - name: Summarize safe facts
        run: |
          python3 - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import json

          report = json.load(open("phala-preflight.json", encoding="utf-8"))
          print("### Phala read-only preflight")
          print()
          print(f"- API version: `{report['apiVersion']}`")
          print(f"- Shape: `{report['instanceType']}` / {report['vcpu']} vCPU / {report['memoryMb']} MB / {report['diskSizeGb']} GB")
          print(f"- Hourly price (USD micros): `{report['hourlyPriceUsdMicros']}`")
          print(f"- Available matching nodes: `{report['availableNodeCount']}`")
          print(f"- Finite apps: `{report['finiteAppCount']}`")
          print(f"- Non-deleted Finite CVMs: `{report['nonDeletedFiniteCvmCount']}`")
          print(f"- Finite revisions: `{report['finiteRevisionCount']}`")
          print(f"- Conservative billable resources: `{report['billableFiniteResourceCount']}`")
          print()
          print("This workflow performs authenticated reads only. It does not provision, update, stop, restart, or delete a CVM.")
          PY
